# Ship note — M2: trajectories on the page, and the R0 review backlog (2026-09-22)

Status: **deployed 2026-09-22 07:35 local** (`dailybrief-rs:m2`, commit bfa909a plus the
Task 15 fixes); `/ship` review pending. Rollback: `docker compose --profile app down &&
DAILYBRIEF_IMAGE=dailybrief-rs:r0 docker compose --profile app up -d` (ADR 0015).

## What shipped

- `harness::trajectory`: a typed `Event` over every stored `stream-json` line with a
  raw-keeping `Unknown`; turns folded by assistant message id; caps used against the
  configured limits; the retry chain (ADR 0012). Lines travel adapter → writer over a bounded
  channel and cannot be dropped.
- `/runs/{id}`: caps bar, retry chain, one row per turn; `/runs` with turns and wall clock;
  the day page's failed and running states carry the caps bar. `dailybrief run --max-turns`.
- Harness: token redaction before storage, `runs.error` scanned, lossy stdout reads, SIGKILL
  after grace and killed-run transcripts proven, a fourth publish after `fatal` refused.
- Retention: `dailybrief prune [--days] [--dry-run]`; one scheduler loop drives the 06:30 run
  and the 07:00 prune (ADR 0013).
- Web: `/run/status` from the lock holder; the manual cap counts runs; JWKS forced refetch
  throttled; HS256 confusion pinned by test.
- Core: one `(id, vector)` projection per ingest batch; 14-day title window; concurrent
  `fetch_sources` fetches once; the extractor is logged for ADR 0014.
- Ops: vendored, sha256-pinned installer with `curl`/`zstd` purged (527 MB image); licence
  lists kept identical by test; `harness.mcp_command` dropped; `DAILYBRIEF_IMAGE` rollback
  variable; `docs/runbook.md`; ADR 0015.

## `spec/m2.md` §9 criteria

| # | Criterion | Evidence |
|---|---|---|
| 1 | every turn visible | `run_page_renders_caps_retry_and_every_turn` (snapshot of the 77-row table for `2026-09-21-c8354589`; caps `reads 32/45 · selects 31/30 hit · web 0/5 · turns 77/120 · 8:20/15:00 · attempt 1 of 2`); `fixture_turns_reconcile_with_the_result_line` |
| 2 | forced failure renders correctly | real run `2026-09-22-7494d2ef` on the live service: `runs.status = failed`, `error_max_turns`, 5 assistant messages / `num_turns` 6, 44.6 s, `scan-transcript` clean; transcript captured as `tests/fixtures/transcripts/max-turns-5.jsonl` and `forced_failure_renders_turns_hit_on_home_and_run_page` replays it (`turns 5/5` hit, run link, five rows). Today's page shows the 06:30 digest, not the failure, because a digest wins (`SPEC.md` §7b) |
| 3 | retention works and is bounded | `prune_*` tests; first live prune 2026-09-22 00:42 UTC: dry-run then real, 0 runs older than 60 days (the oldest run is 2026-09-16); `/data/runs` 25 directories, 21.5 MB |
| 4 | backlog closed by name | all 18 tests of §9 #4 exist and pass (`bin/check full`, 284 tests); `readability` decision deferred to the M3 `/plan` with the measurement window starting 2026-09-23 (ADR 0014) |
| 5 | image and docs | `docker history`: vendored installer, no `curl`/`zstd` (`command -v` fails in the image); `deny_licences_match_constraints`; `config.toml` has no `mcp_command`; `docs/runbook.md`; `dailybrief-rs:r0` on the box (rebuilt from a5f985c, ADR 0015) |
| 6 | gates | `bin/check full` green at Checkpoint D (95.61 % core / 82.58 % rest, above R0); CI green on every commit; deployed 07:35 local, after the morning run; rollback target `r0` |

## Operations on ship day

- 06:30 scheduled run `2026-09-22-de8ebfdb` (still on the R0 image): success, 93 turns,
  24 + 6 — R0 criterion 8, morning 1 of 3.
- 07:35 deploy of `dailybrief-rs:m2`; loopback 401, public 302 to Access, scheduler armed.
- 07:41 gate run `--attempts 1 --max-turns 5` (one subscription run, pre-approved).
- 07:42 first prune, then `docker rm dailybrief-app-1` (the TypeScript container; its
  volumes `dailybrief_dailybrief-*` are kept until M3).

## Open

- Phone check of `/runs/2026-09-22-7494d2ef` and of the caps bar (Checkpoint B).
- R0 criterion 8 mornings 2 and 3 (2026-09-23, 2026-09-24), now on the M2 image.
- `/review` and `/ship` for M2.
