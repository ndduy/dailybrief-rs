# Ship note — R0 first deployment (2026-09-18)

Status: **cut over on 2026-09-21 05:22 UTC**. The Rust service serves
`dailybrief.hundredclouds.com` behind Cloudflare Access; the TypeScript container is stopped
(not removed) and remains the rollback target.

## What was done (in order, all on the box)

1. `docker stop dailybrief-app-1` (the TypeScript service; project `dailybrief`).
2. Copied `brief.db` and `runs/` from `dailybrief_dailybrief-data` into
   `dailybrief-rs_dailybrief-data` (helper container, `cp -a`, then
   `PRAGMA wal_checkpoint(TRUNCATE)`; `integrity_check` = ok). The fastembed model cache was
   copied from the smoke volume so the first run did not download 128 MB again.
   Snapshot: 490 items (all with vectors), 13 topics, 8 digests, 13 runs, ledger `0001_init`.
3. `dailybrief migrate` in the runtime image: applied nothing; `sha256(brief.db)` before and
   after = `4d3026c5c15d72ae5c53758925be9901492b82a94d19f6aaf224a51b7701a1a1` (criterion 5).
4. `dailybrief reembed`: 490 items + 13 topics in 26 s.
5. `dailybrief fetch`: 36/36 feeds (22 ok, 14 not-modified), 437 new items; 927 items, all
   with vectors.
6. Smoke run (`prompts/smoke.md`, `schemas/smoke.json`, `--no-verify --attempts 1`):
   run `2026-09-18-9f96bb14`, success, 3 turns, 4 s. The transcript's `system/init` lists
   `mcp_servers: [{name: "dailybrief", status: "connected"}]` and the tools
   `StructuredOutput`, `WebSearch` and the nine `mcp__dailybrief__*`; `apiKeySource: "none"`
   (criterion 6; `StructuredOutput` is the built-in that `--json-schema` adds).
   `scan-transcript`: clean.
7. Real run: `2026-09-18-9f4cc78e`, `runs.status = success`, attempt 1 of 2, 84 turns,
   33 reads, 10 min 17 s wall clock; `digests` row `2026-09-18-2026-09-18-9f4cc78e` with
   24 for-you + 6 beyond-radar; `scan-transcript` clean. The `result` line is
   `docs/ship/r0-run-2026-09-18-9f4cc78e-result.json` (criterion 7). Tool calls:
   get_briefing 1, fetch_sources 1, list_candidates 3, read_item 33, search_items 8,
   select 31, editor_notes 3, report_feed_issue 1 (google-testing: sidebar-only
   extraction), publish_digest 1. Informational `total_cost_usd` 6.80 (list price; billed to
   the Max subscription, not an API key).
8. `docker compose --profile app up -d`: the service **refused to start**:
   `CF_ACCESS_AUD is unset and the bind is 0.0.0.0: refusing to serve unauthenticated off
   loopback`. Both repos' `.env` had the two Access values empty; the TypeScript build never
   verified the JWT itself. `CF_ACCESS_TEAM=shrill-tree-5c46` was filled from the public
   Access login redirect. The **application audience tag** is only in the Zero Trust
   dashboard (Access → Applications → Daily Brief → Overview → Application Audience (AUD) Tag).
9. Rollback executed (criterion 11): `docker compose --profile app stop` (Rust) then
   `docker compose --project-directory ../dailybrief --profile app up -d` (TypeScript).
   Loopback `GET /` → 200 from the TypeScript service; public URL → 302 to Access.

Two defects were found and fixed on the way, each with a test, in commit `f5061a8`:
the image shipped a half-initialised Claude config dir (Claude Code refused to start), and
relative `--prompt`/`--schema` overrides were not pinned to the invoking cwd.

## Cut-over (executed 2026-09-21)

`CF_ACCESS_AUD` was added to `.env` on 2026-09-21. Because three TypeScript mornings had
passed since the snapshot, step 2 was repeated first: the TypeScript stack stopped, its
`brief.db` (604 items, 10 digests, 17 runs, 6 reads, latest digest 2026-09-20) and `runs/`
copied over, WAL checkpointed, `integrity_check` ok; `migrate` byte-identical
(`90b64dcf…4a12f`); `reembed` 604 items + 13 topics; `fetch` 36/36, 432 new items. The
Rust-side rows from the 2026-09-18 verification runs are therefore no longer in the
database (their transcripts stay under `/data/runs/`, and the result line is in this folder).
After `up -d`: loopback `GET /` and `POST /run` → 401, public URL → 302 to Access, log
`Cloudflare Access verification on` with issuer `shrill-tree-5c46`, scheduler armed for
`30 6 * * *` (next 2026-09-21 23:30 UTC).


```sh
docker compose --project-directory ../dailybrief --profile app stop \
 && docker compose --profile app up -d \
 && curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8788/   # expect 401
```

The adopted database is a copy taken at 04:49 UTC on 2026-09-18. Anything the TypeScript
service writes after that (its own 06:30 run) is not carried over; cut over before the next
morning run or repeat step 2.

## Rollback (one command)

```sh
docker compose --profile app down && docker compose --project-directory ../dailybrief --profile app up -d
```

## R0 criteria (`spec/r0.md` §9)

| # | Criterion | Evidence |
|---|---|---|
| 1 | `bin/check full` green in the test container at the ship commit | box: `full: ok in 151s`; CI run 35303550178 `full: ok in 1496s` |
| 2 | MCP integration tests | `tests/it/mcp.rs`, `tests/it/mcp_stdout.rs` (part of the 211) |
| 3 | adapter + runner tests, argv snapshot | `tests/it/harness.rs`, `tests/it/runner.rs`, `src/harness/claude_code.rs` snapshot |
| 4 | `gen-schemas --check` green | in `bin/check task` and CI |
| 5 | migrate is a byte-identical no-op on the live copy | step 3 |
| 6 | smoke run init line | step 6 |
| 7 | real run success, 24 + 6, scan clean, result line attached | step 7 |
| 8 | three consecutive scheduled 06:30 runs | **open** — window starts 2026-09-22 06:30 (+07:00); dates go here |
| 9 | phone: opens after OTP, one tap → `reads` row | **open** — cut over; awaiting the tap |
| 10 | no API key anywhere; env-scrub test; `docker history` clean | `tests/it/compose.rs`, `web::auth` tests, CI step "Runtime image carries no secret" |
| 11 | rollback documented and executed once | step 9 |

## Notes

- `docker compose` warns that `dailybrief-rs_dailybrief-data` "was not created by Docker
  Compose" because step 2 created it with `docker volume create`. Harmless; it is used as is.
- Two failed manual runs (`2026-09-18-726aefbf`, `2026-09-18-861c3d43`) sit in `runs` from the
  first two smoke attempts; they are honest history and render as failed on `/d/2026-09-18`
  only if no digest existed, which is not the case.
