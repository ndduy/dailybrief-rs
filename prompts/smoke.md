# Smoke test

You are checking that the `dailybrief` tools are reachable. Call `get_briefing` once. Then answer with a single JSON object `{ "date": "<the date it returned>" }` and nothing else. Do not call any other tool and do not use web search.
