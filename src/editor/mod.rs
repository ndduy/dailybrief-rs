//! The Editor contract (`SPEC.md` §4): the final-message type behind `schemas/digest.json`, the
//! byte-stable prompts, and the per-run `mcp.json` rendering.

pub mod digest_output;
pub mod mcp_config;
pub mod prompt;
